name: shared_core
description: A starting point for Dart libraries or applications.
version: 1.0.0
resolution: workspace

environment:
  sdk: '^3.11.0'

dependencies:
  nsd: ^4.1.0
  multicast_dns: ^0.3.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
